<h1>GWARstagRAM!!!</h1>

<div id="entires-list">
  <% @entries.each do |entry| %>
    <%= render partial: 'entry', object: entry %>
  <% end %>
</div>
<h3>Create New Sacrifice</h3>
<%= render 'form', entry: @entry %>
